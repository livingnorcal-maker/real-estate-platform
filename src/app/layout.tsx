import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "HomeDirectly | Buy & Sell Real Estate Without Agents",
    template: "%s | HomeDirectly",
  },
  description:
    "Buy and sell real estate directly. No agents, no commissions. Connect with buyers and sellers on your terms.",
  keywords: [
    "real estate",
    "buy home",
    "sell home",
    "FSBO",
    "for sale by owner",
    "no agent",
    "peer to peer real estate",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster richColors position="top-right" />
      </body>
    </html>
  );
}
